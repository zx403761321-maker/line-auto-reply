# =====================================================
# LINE CRM — 云手机设备配置
# =====================================================
# 当前只用云手机，加新设备只需加一条记录，然后 POST /device/register

devices:
  - id: cloud-01
    name: "云手机-OPPO"
    type: cloud
    adb_host: "39.109.41.52"
    adb_port: 499
    line_account: "account-01"
    enabled: true

  # 扩量时取消注释：
  # - id: cloud-02
  #   name: "云手机-02"
  #   type: cloud
  #   adb_host: "x.x.x.x"
  #   adb_port: 500
  #   line_account: "account-02"
  #   enabled: false

adb_settings:
  connect_timeout: 10
  command_timeout: 15
  reconnect_on_failure: true
  max_reconnect_attempts: 3
